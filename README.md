To use the memory_card.py application, you must do the following steps:

1) Type pip install PyQt5 if not already downloaded (Please download the module online if download wheel does not have it pre-installed)
2) Put project into a folder on the desktop
3) Navigate to file and start
